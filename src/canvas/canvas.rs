use crate::canvas::*;

// pub struct Canvas {
// size:  Size,
// cells: Vec<DamageCell>,
// }
//
// impl Canvas {
// pub fn new(size: Size, cell: Cell) -> Self {
// let len = size.x * size.y;
//
// let mut cells = Vec::with_capacity(len);
// cells.resize(len, cell.into());
//
// Self { size, cells }
// }
// }
